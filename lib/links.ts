/**
 * Off-site destinations that more than one component needs.
 *
 * Applications live on HARP, the HackUTD registration portal, rather than
 * anywhere in this site - so the nav's Apply button and the footer's Register
 * button are two labels for one destination and must not drift apart.
 */
export const APPLY_URL = "https://harp.hackutd.co";
